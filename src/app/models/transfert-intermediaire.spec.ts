import { TransfertIntermediaire } from './transfert-intermediaire';

describe('TransfertIntermediaire', () => {
  it('should create an instance', () => {
    expect(new TransfertIntermediaire()).toBeTruthy();
  });
});
